# 小羽二代 Wingie2 用户手册 · v4

> **注意**
> - 设备启动时，请从低音量起步，逐渐调大音量；
> - 本设备易与音箱和耳机产生回授，注意避免刺耳高频；
> - 存放与使用请置于干燥处，远离潮湿；
> - 请勿直接输入模块合成器或其他高电平信号。

感谢购买小羽二代！

小羽二代是一个手持立体声共鸣器，它自带麦克风，可以即时与人声和周围的环境产生关系，也可以连接其他设备，或作为开发平台使用。其体量轻巧，易于携带。让你随时随地创作音乐、探索声音。

---

## 目录

1. [电源](#1-电源)
2. [音频输入 / 输出](#2-音频输入--输出)
3. [面板控件](#3-面板控件)
4. [模式](#4-模式)
5. [自定义山洞](#5-自定义山洞)
6. [音序器阈值](#6-音序器阈值)
7. [前级与后级增益](#7-前级与后级增益)
8. [MIDI 通道与控制信息](#8-midi-通道与控制信息)
9. [USB 网页配置](#9-usb-网页配置)
10. [MPE 模式](#10-mpe-模式)
11. [USB 网页刷机](#11-usb-网页刷机)
12. [替代律制](#12-替代律制)
13. [全局设置（MIDI CC 表）](#13-全局设置midi-cc-表)
14. [存储设定](#14-存储设定)
15. [山洞模式与替代律制](#15-山洞模式与替代律制)
16. [使用窍门](#16-使用窍门)
17. [版本说明](#17-版本说明)

---

## 1. 电源

小羽使用 USB Type-C 口进行供电。可以使用充电器或充电宝供电。第一批小羽二代（2022 年 5 月之前生产，背后没有螺丝）不可使用 C-C 线，只可使用 A-C 线。

如果输出出现数字噪音，可以尝试更换电源或使用地线隔离器。

接通电源之后，会有大概 3 秒钟的时间从静音到全音量淡入。

## 2. 音频输入 / 输出

### 音频输入

小羽灵敏的麦克风拾取空气中的声音，非常容易与扬声器产生回授。你可以演奏回授。v4 版本固件（带有第五种**比例模式**的）拥有抗啸叫功能，回授仍然可以建立，但是会大幅降低失真、得到柔和音质。不想要回授可以使用耳机，注意控制音量以避免与耳机回授。

你可以使用小羽聆听身边的声音，把小羽当作一个打击乐器来使用，或者用它把任何物体变成一件乐器。开动脑筋，尽情实验。

小羽的音频输入是立体声 3.5mm 接口（TRS）。线路输入可设为立体声或单声道：单声道时左右线路先混合成一轨，再同时进入左右两个共鸣器。麦克风不受此设定影响，始终保持立体声。开机时同时按住左右音符键盘的 C，可在立体声与单声道之间切换，并立刻写入闪存。也可在 [USB 网页配置](#9-usb-网页配置) 的「线路输入」中修改（立即改变运行状态，Save 后才写入闪存）。出厂默认为立体声。

> **注意**：避免过大的输入信号。过大的输入信号会使过载的原声出现在输出中。如果在音源选择切换到麦克风时，或在干湿比设定为 100% 时，仍然可以听到线路输入的声音，就说明线路输入信号过大了。这时请调低输入信号电平（不是小羽上的推子）。

### 音频输出

小羽的音频输出是立体声 3.5mm 接口（TRS）。可直接驱动耳机。

## 3. 面板控件

控制功能已经被标注在小羽上。

混合与音量都控制共鸣器前的信号强度，这个设计允许共鸣器的衰减段可以被完整听到。

当左右声道拥有相同的八度设定时，右声道比左声道高一个八度。

| 推杆 | 功能 |
|------|------|
| 混合 | 干湿声音比例 |
| 衰减 | 共鸣器的衰减时长，从 0.15 秒到大约 10 秒 |
| 音量 | 输入音量 |

## 4. 模式

小羽拥有 **五种** 模式，按下模式按键循环选择。当前模式通过 LED 显示。

| 模式 | 复音数 | 音符键盘 | 八度开关响应 | LED |
|------|--------|----------|--------------|-----|
| **复音**（白色） | 三复音 | 按键循环控制每个复音 | 影响下一个按下的音符。可以在演奏音符之前切换八度，以混合不同八度的音符 | 白 |
| **琴弦**（黄色） | 单复音 | 同时按下多个音符按键设定音序；每当输入音量超过阈值时音序步进 | 即时 | 黄 |
| **音块**（红色） | 单复音 | 同上 | 即时 | 红 |
| **山洞**（紫色） | 九共鸣器（三个八度档切换） | 见[第 5 章](#5-自定义山洞) | 切换山洞（一共三个） | 紫 |
| **比例**（白黄交替闪烁） | 三复音 | 按键循环控制每个复音；共鸣器频率 = 基频 × 比例 | 影响下一个按下的音符 | 白黄交替闪烁 |

全局调音影响 复音、琴弦、音块、山洞 和 比例 五个模式。（出厂设定 A3 (69) = 440 Hz）。如需进行全局调音，请见[第 13 章](#13-全局设置midi-cc-表)。

### 比例模式（Ratio Mode）

比例模式是 v4 新增的第五种模式。与按八度堆叠的复音/琴弦/音块不同，比例模式让每个共鸣器的频率等于 **基频 × 比例**，因此可以产生非八度音程的共鸣关系。

- **三声部复音**：9 个共鸣器分为 3 个槽（声部 1 用共鸣器 1/2/3，声部 2 用 4/5/6，声部 3 用 7/8/9）。按一个音符占用一个声部，3 个声部全占时替换最老的一个。
- **左右共用同一份比例**：左右声道使用同一组 9 个比例，只是基频（八度）不同。出厂比例为 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 / 9。
- **比例范围**：0.125 至 32.0，步进 0.001。频率被限制在 16.00–16000.00 Hz。比例通过 [USB 网页配置](#9-usb-网页配置) 或 MIDI 调整。

进入比例模式有三种方式：

1. **面板按键**：在复音 → 琴弦 → 音块 → 山洞之后继续按一下，即进入比例模式（第 5 下）。
2. **MIDI**：CC 0 值 103–127。
3. **USB 网页配置**：在配置页的左右模式下拉中选择"比例"。

## 5. 自定义山洞

山洞模式下，每个声道有 9 个共鸣器，分为三个八度档（bank）。可以逐个调整每个共鸣器的频率和静音状态。

- 切换静音 / 非静音状态
- 增加对应共鸣器的频率
- 降低对应共鸣器的频率
- 所有共鸣器解除静音
- 所有共鸣器静音
- 左右通道可以分别调音

![自定义山洞面板操作](img/cave_controls.jpg)

频率范围 `16.00–16000.00 Hz`，分辨率 `0.01 Hz`。频率既可由面板在山洞模式下调节，也可由 MIDI CC（[第 13 章](#13-全局设置midi-cc-表)）或 [USB 网页配置](#9-usb-网页配置) 调节。

## 6. 音序器阈值

琴弦和音块模式下，每当输入音量超过阈值时音序步进。阈值可左右分别设置。

- 右通道音序器阈值
- 左通道音序器阈值

**在面板上设定**：按住对应声道的模式按键，再按音符按键选择阈值。按键从 C 到 B 按半音阶排列，对应 12 档——C 为最低，B 为最高。

![音序器阈值面板操作](img/seq_threshold.jpg)

阈值也可由 MIDI CC 或 [USB 网页配置](#9-usb-网页配置) 设置（对应"输入阈值"字段）。

## 7. 前级与后级增益

过载效果器（clipper）的前级和后级增益可以分别调节：

- **后级增益**：共鸣器音量
- **前级增益**：调节饱和度

**在面板上设定**：按住对应声道的模式按键，再按音符按键选择增益。按键从 C 到 B 按半音阶排列，对应 12 档——C 为最低，B 为最高。

![前级与后级增益面板操作](img/clip_gain.jpg)

调节前级增益获得想要的音色，然后调节后级增益获得舒适的干湿比控制响应。

增益也可由 MIDI CC 或 [USB 网页配置](#9-usb-网页配置) 设置。

## 8. MIDI 通道与控制信息

小羽可以被 MIDI 控制。MIDI 接口按照 MMA 标准设计，请使用标准 MIDI 连线。

![标准 MIDI 连线](img/midi_wiring.jpg)

### MIDI 通道

| MIDI 通道 | 音符 | 控制信息（CC） |
|-----------|------|----------------|
| 用户可调（出厂设定 1、2、3） | 控制左声道<br>控制右声道<br>交替控制左右声道 | 控制左声道<br>控制右声道<br>同时控制左右声道 |

出厂默认：左声道 = 通道 1，右声道 = 通道 2，左右共用 = 通道 3。可在 [USB 网页配置](#9-usb-网页配置) 或通过 MIDI CC（[第 13 章](#13-全局设置midi-cc-表)）修改。

### MIDI 音符

- 小羽接受 Note On 信息（velocity 0 当作 Note Off）。
- 内置音序器只可用于本体上的迷你键盘，在 MIDI 控制下无效。
- 快速且较大音程的音符跳动可能导致大音量声音。

### 控制信息（CC）

- MIDI CC 输入会覆盖相对应推杆设定，再次挪动推杆使推杆设定有效。
- 小羽可接受 14-bit MIDI 信息。

**CC 0 — 模式选择**（v4 五模式均分）：

| CC 0 值 | 模式 |
|---------|------|
| 0–25 | 复音 |
| 26–51 | 琴弦 |
| 52–76 | 音块 |
| 77–102 | 山洞 |
| 103–127 | 比例 |

> v4 起 CC 0 由原来的四档（0-31 / 32-63 / 64-95 / 96-127 + 127 单点）改为五模式均分。如果你之前用固定 CC 值切模式，请按上表重新核对。

**推杆 CC**：

| CC | 功能 |
|----|------|
| CC 11 (MSB) / CC 43 (LSB) | 混合（干湿比） |
| CC 1 (MSB) / CC 33 (LSB) | 衰减时间 |
| CC 7 (MSB) / CC 39 (LSB) | 音量 |

## 9. USB 网页配置

[https://mengqimusic.github.io/Wingie2](https://mengqimusic.github.io/Wingie2) 打开后选择"配置"，即可用浏览器通过 USB 直接连接小羽，修改和保存所有设置。它不需要 Wi-Fi、SoftAP、CDN、Node 或 Python，只需要一个支持 Web Serial 的浏览器。

### 连接方式

1. 使用桌面版 **Chrome** 或 **Edge**（需支持 Web Serial），从 **HTTPS** 地址打开页面。Safari、Firefox、移动浏览器和非安全 HTTP 页面不支持。
2. 关闭 Arduino Serial Monitor 等占用小羽串口的软件。
3. 点击"连接 Wingie2"，选择对应的 USB 串口。

### 能改什么

连接后，页面会读取一次完整设备快照。你可以修改以下设置，**每次有效编辑都立即作用于正在运行的声音，不需要 Apply**：

- **左右声道**：模式（复音 / 琴弦 / 音块 / 山洞 / 比例）、输入阈值
- **共享设置**：A3 频率（358.08–521.91 Hz）、调律（标准 + 8 种替代律制）、前级 / 后级增益、三个 MIDI 通道路由、MPE 开关、线路输入立体声 / 单声道
- **比例模式设定**：9 个共鸣器比例（分 3 槽），可"复制槽 1 到槽 2、3"，可恢复出厂比例
- **山洞**：左右各 3 个 bank，每个 bank 9 个共鸣器的频率和静音

> 页面**不显示**混合、衰减、音量——它们是面板上的实体推杆，不写入闪存。

### 保存

改好的声音只有在点击 **"Save to Flash"** 并确认后，才会写入闪存、在重启后保留。在此之前，所有改动只影响当前运行状态。

### 同步

如果小羽的面板控件、MIDI 或其他软件改变了设备状态，页面会在空闲时自动把变化同步过来。你也可以随时点击 Refresh 立即重读。

### 嵌入网页

若页面放在 iframe 中，iframe 需要 `allow="serial"`，服务器也必须允许相应的 `Permissions-Policy: serial=(self)`。

## 10. MPE 模式

MPE（MIDI Polyphonic Expression）让支持 MPE 的控制器对小羽的每个音做独立的弯音控制。v4 的 MPE 由 [USB 网页配置](#9-usb-网页配置) 的开关控制，**出厂默认关闭**，状态保存在闪存。

### 开关关闭——常规路由

没有 Zone。通道 1–16 全部走可配置的常规左 / 右 / 共用路由（出厂默认 1 / 2 / 3）。通道 13 调律、14 / 15 山洞频率、16 全局设置 CC 都可以正常使用。

### 开关开启——标准 Lower Zone

一个 Lower Zone 占用全部通道：**Manager = 通道 1，Member = 通道 2–16**。

- **音符分配**：每个 Note On 按到达顺序在左右引擎间交替分配（与常规"共用"路由相同的轮转方式）。如果某一侧在山洞模式，则跳过该侧；两侧都山洞则该音不响。
- **逐音弯音**：每个 Member 通道的 Pitch Bend 只影响该通道当前拥有的那些音；Manager（通道 1）的 Pitch Bend 和 CC 对两侧所有 MPE 音全局生效。
- **弯音范围**：Member 默认 ±48 半音，Manager 默认 ±2 半音。可用 RPN 0（CC 6 设半音、CC 38 设音分）调整，在任一 Member 通道收到后应用到所有 Member。
- **外部替代调律**：MPE 源可以在每个音的 Note On 之前发送 Pitch Bend，为该音建立初始的微音分偏移。此时建议把小羽内部调律设为"标准"，让 MPE 源成为唯一调律权威；若内部仍开着替代律制，内部音程与 MPE 偏移会叠加。

开启 MPE 后，常规的左 / 右 / 共用路由不再适用，通道 13–16 的控制 CC 也被 Zone 接管（调律用逐音 Pitch Bend，山洞 / 全局设置走网页配置）。

### 逐音压力表情（0xD0）

此前一版曾把 Member 通道的 Channel Pressure（0xD0）映射为逐音衰减叠加（最多 +3 秒）。该热路径会在开机约 10 秒后触发看门狗复位，已从音频核撤回：0xD0 仍被消费，但不再改变衰减，待更轻的实现再接回。非 MPE 模式下，路由通道的 Channel Pressure 仍按弯音的既有先例作用于整侧。

Member 的 CC 74 与其余 CC 被消费但不映射。双 Zone 控制器的两个 Zone 都会并入小羽的单 Zone。

详见 [`MPE.md`](MPE.md) 与 [`ALT_TUNING.md`](ALT_TUNING.md)。

## 11. USB 网页刷机

[https://mengqimusic.github.io/Wingie2](https://mengqimusic.github.io/Wingie2) 打开后选择"固件安装"，即可安装或升级小羽固件。刷机页直接连接 ESP32 ROM bootloader，因此适用于空白 Flash、旧版本升级，以及应用固件损坏但 bootloader 正常的设备。

### 操作步骤

1. 用桌面版 **Chrome / Edge**，从 **HTTPS** 地址打开页面。用能传输数据的 USB 线连接小羽。
2. 关闭配置页、Arduino Serial Monitor、DAW / MIDI 工具等占用串口的软件。
3. 点击"连接 Wingie2"，选择小羽的 USB 串口。
4. 显示"设备已连接"后，点击"安装固件"。安装过程中不要拔 USB 或关闭页面。
5. 显示"安装完成"后即可使用。

### 安全边界

标准刷机**只写四个固定区域**（bootloader、分区表、boot_app0、应用固件），**不执行整片擦除，不读取 app0，也不写 `0x9000` 开始的 NVS**。这意味着你的 MIDI 通道、调律、山洞、比例等设置在标准刷机后**全部保留**。

恢复出厂设置或擦除配置不属于此页面的标准流程。

### 故障处理

- **端口占用**：关闭其他配置页、Serial Monitor、DAW / MIDI 工具后重试。
- **找不到串口**：更换可传输数据的 USB 线和 USB 端口，安装设备所需的 USB 串口驱动。
- **无法进入 bootloader**：先让页面自动切换；仍失败时按住 BOOT，短按 RESET/EN，开始连接后再松开 BOOT。
- **错误芯片**：不要继续；本发布包只支持原始 ESP32，不支持 ESP32-S2/S3/C3。
- **写入失败**：不要拔线，先重试当前安装；重复失败时更换 USB 线或端口。

## 12. 替代律制

默认情况下，小羽使用标准的西方律制（十二平均律）。

从 v3.1 版本开始，额外提供了八种律制（其他律制可以通过更改源代码实现）：

- Centaur (Kraig Grady)
- Harp of New Albion (Terry Riley)
- Carlos Harmonic (Wendy Carlos)
- Well-Tuned Piano (La Monte Young)
- Meta Slendro (Grady/Wilson)
- Bihexany (Gene Ward Smith)
- Hexachordal Dodecaphonic (Paul Erlich)
- Augmented[12] (Mike Smith, Paul Erlich)

有三种方式可以启用替代律制：

1. **启动设备时按住左侧的模式按钮**（推子位置决定律制，见下表）。
2. **使用 MIDI**（通道 13，CC 23，值 0–8，见下表）。优点是不需要重启，可在运行时更改。
3. **使用 USB 网页配置**：在共享设置的"调律"下拉中选择标准或八种替代律制之一。同样不需要重启。

替代律制还会影响山洞模式（见[第 15 章](#15-山洞模式与替代律制)），并受全局调音（A3 频率）的影响。

### 启动设备时切换律制

按住左侧的模式按钮并插入 USB 线。推子的位置将确定调音方式：

| 左模式按键 | 右模式按键 | 左推子 | 中推子 | 右推子 | 律制 |
|-----------|-----------|--------|--------|--------|------|
| 松开 | 按住 | — | — | — | 十二平均律 |
| 按住 | 松开 | 底部 | 底部 | 底部 | Centaur |
| 按住 | 松开 | 底部 | 底部 | 顶部 | Harp of New Albion |
| 按住 | 松开 | 底部 | 顶部 | 底部 | Carlos Harmonic |
| 按住 | 松开 | 底部 | 顶部 | 顶部 | Well Tuned Piano |
| 按住 | 松开 | 顶部 | 底部 | 底部 | Meta Slendro |
| 按住 | 松开 | 顶部 | 底部 | 顶部 | Bihexany |
| 按住 | 松开 | 顶部 | 顶部 | 底部 | Hexachordal Dodecaphonic |
| 按住 | 松开 | 顶部 | 顶部 | 顶部 | Augmented[12] |

有关调弦的更多信息，请参阅 `wingie_tuning_notes.pdf`。

### 通过 MIDI 切换律制

| 通道 | CC | 数值 | 律制 |
|------|----|------|------|
| 13 | 23 | 0 | 十二平均律 |
| | | 1 | Centaur |
| | | 2 | Harp of New Albion |
| | | 3 | Carlos Harmonic |
| | | 4 | Well Tuned Piano |
| | | 5 | Meta Slendro |
| | | 6 | Bihexany |
| | | 7 | Hexachordal Dodecaphonic |
| | | 8 | Augmented[12] |

由 Dave Seidel 提供的 TouchOSC 模板：`wingie_tuning.tosc`。

## 13. 全局设置（MIDI CC 表）

MIDI 通道、全局调音、替代律制和山洞模式共鸣器频率可以通过 MIDI 全局设置通道（通道 16）设定，也可以通过 [USB 网页配置](#9-usb-网页配置) 设定。

> 大范围频率跳动会引发大音量声音，调节之前请降低音量和衰减时间。

| MIDI 通道 | MIDI 控制器 | 功能 | 出厂设定 |
|-----------|------------|------|---------|
| 16 | 20 | 左声道对应 MIDI 通道 | 1 |
| | 21 | 右声道对应 MIDI 通道 | 2 |
| | 22 | 左右声道共用 MIDI 通道 | 3 |
| | 23 (MSB) / 55 (LSB) | 全局调音（以 440 Hz 为中心，可调范围 ±81.92 Hz，分辨率 0.01 Hz） | 0.00 |
| 15 | 23–31 (MSB) / 55–63 (LSB) | 右声道当前所选山洞调音 | — |
| 14 | 23–31 (MSB) / 55–63 (LSB) | 左声道当前所选山洞调音 | — |
| 13 | 23 | 替代律制（0–8，见[第 12 章](#12-替代律制)） | 0 |

## 14. 存储设定

有两种方式把当前设置写入闪存，重启后保留：

1. **面板双键存储**：同时按住两个模式按钮三秒。计时中 LED 会在四个颜色中循环，并在计时结束后闪动，以示存储完成。这个操作有可能引发短促的杂音，不推荐在录音或现场演出中进行。
2. **网页 Save to Flash**：在 [USB 网页配置](#9-usb-网页配置) 页面点击"Save to Flash"并确认。

存储的内容包括：A3 频率、调律、前级 / 后级增益、三个 MIDI 通道路由、左右模式与输入阈值、共享比例设定、左右各 3 个山洞 bank 的频率和静音状态、MPE 开关，以及线路输入立体声 / 单声道。开机时同时按住左右 C 切换线路单声道时，会立刻写入闪存，不必再做双键存储。

## 15. 山洞模式与替代律制

用替代调弦时，山洞也会被调整以匹配您选择的调弦。

为了适应所有 12 个音高，在山洞模式中，左声道对应偶数音，覆盖一个又三分之一的八度音程：

`C, D, E, F#, G#, A#, C', D', E'`

右声道对应奇数音，同样覆盖一个又三分之一的八度音程：

`C#, D#, F, G, A, B, A#, C#', D#', F'`

三个位置切换开关切换三个八度音程，类似于复音、琴弦和音块模式。然而，左右山洞始终处于同一个八度音程，因此所有音阶音高都覆盖在两侧。

Dave Seidel 为小羽写了替代律制功能。

## 16. 使用窍门

1. 把小羽当作打击乐器演奏；
2. 携带小羽出门，随环境音演奏；
3. 与各种原声乐器合奏；
4. 与喇叭建立回授；
5. 与效果器建立回授；
6. 与鼓机合用，用鼓声触发音序器，获得旋律；
7. 在现场演出时，使用外部心形拾音话筒和线路输入以避免回授；
8. 用比例模式探索非八度音程的共鸣关系；
9. 用 MPE 控制器对每个音做独立的弯音和微调律；
10. 用网页配置页实时调整山洞频率和比例，无需重启。

……（更多玩法由你探索）

感谢 Roy Parvin 撰写英文介绍及校对说明书，安琪撰写小羽背面赠言，Dave Seidel 为小羽写了替代律制功能。

### 在哪里找到我

- 网站：mengqimusic.com
- B 站：space.bilibili.com/4485929
- 微博：weibo.com/mengqimusic
- Instagram：instagram.com/mengqimusic
- 微信扫码，关注个人公众号「合成少数派」

## 17. 版本说明

**v4.10** 相对 v4.03 的变化：

- 新增单声道线路输入模式：在此模式下，线路输入的左右声道混在一起，进入两个共鸣器，方便使用单声道声源。开机时同时按住左右 C 键以切换模式并存储，也可以在配置网页上设定。本设置不影响内置麦克风。

**v4.03** 相对 v4.02 的变化：

- **配置页未保存警示**：有改动未写入闪存时，页面底部状态行与"保存到闪存"按钮以红色标示，保存成功后恢复。
- **按键保存失败提示**：双键保存失败时 LED 红色闪烁（成功为白色闪烁），串口同时打印失败详情。
- 此前一版曾把 MPE 逐音压力表情放进 DSP 热路径，开机约 10 秒看门狗复位。本版音频核与 v4.02 相同，不含该路径；0xD0 仍被消费，但不改变衰减。

**v4.01** 相对 v4 的变化：

- **比例模式 LED 指示调整**：由四色快速循环改为白色、黄色交替闪烁（每 0.5 秒切换），观感更柔和。

**v4** 相对 v3.1 的主要变化：

- **新增比例模式（Ratio Mode）**：第五种共鸣模式，自定义可调比例，三声部复音，频率比例三复音分别可调。
- **新增 USB 网页配置**：用 Chrome / Edge 通过 USB 实时修改和保存所有设置。
- **新增 MPE 模式**：可选的 MIDI 复音表情，支持逐音弯音和外部替代调律。
- **新增 USB 网页刷机**：浏览器内一键安装 / 升级固件，标准流程保留所有设置。
- **新增抗啸叫功能**：回授仍可建立，但大幅降低失真、得到柔和音质。
- **山洞频率精度提升**：从整数提升到 0.01 Hz 分辨率。
- **替代律制第三种启用方式**：除启动按键和 MIDI 外，可用网页配置切换。
- **CC 0 模式分段改为五模式均分**：0-25 / 26-51 / 52-76 / 77-102 / 103-127（原为四档 + 127 单点，见[第 8 章](#8-midi-通道与控制信息)）。

固件已开源：https://github.com/mengqimusic/Wingie2
